# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/scamvax

# Cloudflare R2（两套命名均支持）
R2_ENDPOINT_URL=https://<account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_BUCKET_NAME=scamvax-audio
# 也支持: R2_ENDPOINT / R2_ACCESS_KEY / R2_SECRET_KEY / R2_BUCKET

# 阿里云 DashScope TTS-VC（两套命名均支持）
DASHSCOPE_API_KEY=your_dashscope_api_key
# 也支持: ALIYUN_API_KEY / ALIYUN_URL

# App
APP_ENV=development
BASE_URL=http://localhost:8000
SECRET_KEY=your_secret_key_here

# Rate limiting
RATE_LIMIT_PER_DEVICE=5        # 每设备每小时最大创建次数
RATE_LIMIT_WINDOW_SECONDS=3600

# Audio constraints
AUDIO_MAX_SIZE_MB=10
AUDIO_MIN_DURATION_S=10
AUDIO_MAX_DURATION_S=20

# Share lifecycle
SHARE_TTL_HOURS=72
SHARE_MAX_CLICKS=50
